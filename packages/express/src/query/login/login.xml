<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="fruits">
	<select id="testParameters">
		SELECT
		*
		FROM
		center.enterprise
		WHERE
		enterprise_code = 'co_sl_s312'
	</select>
	<update id="testpost">
		UPDATE center.enterprise
		SET
		enterprise_cod = 'co_sl_s312'
		WHERE
		enterprise_cod = 'co_sl_s312'
	</update>
</mapper>
